# 1.值传递
def f1(n):  # 形参
    n = 'abc'   # 重新赋值

# 实参
a1 = (234,5,7)
f1(a1)
print(a1)

# 2.引用传递
def f2(n):  # 形参
    n[0] = 100  # 修改其中的元素

# 实参
a2 = [234,5,7]
f2(a2)    # n = a2,引用赋值
print(a2)   # [100, 5, 7]
# 实参是可变的数据类型，在函数内部对该数据内部的元素进行修改，想要对实参没有影响的话，则需要拷贝